{
  "alwaysThinkingEnabled": true,
  "permissions": {
    "_comment_deny": "Explicitly blocks access to sensitive files and directories for all Claude Code tools.",
    "deny": [
      "Read(./.env)",
      "Read(./.env.*)",
      "Read(./secrets/**)",
      "Read(./config/credentials.json)",
      "Read(./build)",
      "Read(~/.ssh/**)",
      "Read(~/.aws/credentials)",
      "Read(~/.gcp/**)",
      "Read(~/.config/gcloud/**)"
    ]
  },
  "sandbox": {
    "_comment_enabled": "Enables the sandboxing runtime (Seatbelt/Bubblewrap).",
    "enabled": true,

    "_comment_allowUnsandboxedCommands": "Disables the escape hatch. If a command fails inside the sandbox, Claude Code might normally attempt to retry it. Setting this to false ensures that all commands must run sandboxed.",
    "allowUnsandboxedCommands": false,

    "filesystem": {
      "_comment_denyWrite": "Explicitly blocks write access to sensitive configuration files to prevent persistence mechanisms.",
      "denyWrite": [
        "~/.bashrc",
        "~/.zshrc",
        "//bin",
        "//usr/bin",
        "//etc",
        "~/.ssh"
      ],

      "_comment_denyRead": "Blocks read access to sensitive directories like ~/.ssh and cloud credentials to prevent exfiltration.",
      "denyRead": [
        "~/.ssh",
        "~/.aws/credentials",
        "~/.gcp",
        "~/.config/gcloud"
      ]
    },

    "network": {
      "_comment_allowedDomains": "Creates an explicit allowlist for network access, severely limiting an attacker's ability to exfiltrate data.",
      "allowedDomains": [
        "github.com",
        "api.github.com",
        "registry.npmjs.org",
        "pypi.org"
      ]
    }
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/gh-safeguard.sh"
          }
        ]
      }
    ]
  }
}
